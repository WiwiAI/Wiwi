# Contributing to Wiwi

Спасибо за интерес к проекту 💜

## Как помочь

- сообщать о багах;
- предлагать улучшения;
- улучшать документацию;
- отправлять pull request с фиксом.

---

## Быстрый старт для разработки

```bash
git clone https://github.com/WiwiAI/Wiwi.git
cd Wiwi
python -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -e .[dev]
```

Запуск:

```bash
python -m wiwi
```

---

## Стиль кода и проверки

Перед PR желательно прогнать:

```bash
ruff check .
black .
pytest
```

---

## Pull Request правила

1. Сделай отдельную ветку от `main`.
2. Опиши **что** изменено и **зачем**.
3. Если меняется поведение — добавь пример использования.
4. Не включай секреты, токены, ключи, `.env`.
5. Старайся делать небольшие, сфокусированные PR.

---

## Коммиты

Желательно короткие и понятные сообщения:

- `fix: ...`
- `feat: ...`
- `docs: ...`
- `refactor: ...`

---

## Безопасность

Если нашёл уязвимость — не публикуй её в публичном issue.
Используй инструкцию из `SECURITY.md`.
